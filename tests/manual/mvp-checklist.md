# MVP 手动检查清单

每个发布候选版本都应完整执行本清单。记录 Windows 版本、程序版本、鼠标型号、测试人员和日期；任何失败项都要附上复现步骤。

## 准备

- 使用至少一只有中键、按键 4 和按键 5 的鼠标。
- 准备普通窗口、浏览器、文件管理器、全屏视频和一款全屏游戏。
- 准备 Windows 10 22H2 x64 和 Windows 11 x64 测试机；两台机器均不单独安装 AutoHotkey。

## 启动与状态

| 编号 | 操作 | 预期结果 |
|---|---|---|
| GEN-01 | 启动程序 | GUI、系统托盘和鼠标功能正常启动，只出现一个运行实例 |
| GEN-02 | 关闭和打开“启动 Win Mouse Fix”拨动开关 | 关闭时核心停止并恢复鼠标原始行为；打开时核心启动并恢复已保存设置 |
| GEN-03 | 关闭设置窗口后使用已设置的鼠标动作 | 窗口隐藏到托盘，Win Mouse Fix 继续运行 |
| GEN-04 | 从托盘菜单停止和启动 Win Mouse Fix | 通用页拨动开关、顶部状态和实际鼠标行为保持一致 |
| GEN-05 | 从托盘菜单点击“退出” | GUI、托盘和 AutoHotkey v2 核心全部退出 |
| GEN-06 | 依次切换顶部“通用、按钮、滚动、关于” | 窗口高度平滑匹配页面内容；页面内不重复显示大标题；顶部只显示居中的页面选择器和右侧状态，不显示品牌图标或名称 |
| GEN-07 | 首次启动程序 | 窗口在当前屏幕中间打开，并且不超出屏幕边界 |
| GEN-08 | 依次查看四个页面的主要内容区域 | 录入框、映射表、说明和设置区域均为约 620px，左右边界保持一致 |
| GEN-09 | 连续启动 `WinMouseFix.exe` 两次 | 第二次不创建新的设置窗口、托盘图标或鼠标核心，已有窗口被显示到前台 |
| GEN-10 | 打开“关于”页 | 显示版本、作者胡文凯、个人非商业使用许可、项目主页和三个操作按钮 |
| GEN-11 | 关闭“获取 Beta 版更新”后点击“检查更新” | 先检查 GitHub，只比较正式版本；有新版时显示来源并询问是否打开下载页面 |
| GEN-12 | 开启“获取 Beta 版更新”后点击“检查更新” | 正式版和 Beta 版都参与比较，选择版本号最高的可用版本 |
| GEN-13 | 运行 `WinMouseFix.exe --background` | 设置窗口不显示；托盘和鼠标核心正常启动，从托盘打开后显示设置窗口 |
| GEN-14 | 开启“登录 Windows 后运行”，注销并重新登录 | Win Mouse Fix 静默进入托盘，设置窗口不自动弹出，鼠标设置正常生效 |
| GEN-15 | 分别在 Windows 缩放比例 100%、125%、150% 下查看通知区域 | Windows 从 ICO 中选择 16×16、20×20、24×24 对应资源，鼠标主体大小与相邻通知区域图标接近且边缘清楚 |
| GEN-16 | 查看通用、按钮、滚动、关于页面及按钮选项窗口中的所有按钮和开关 | 普通按钮统一为白底描边，主操作统一为蓝底白字；滚动页的“反向滚动”和“基础平滑”使用与通用页相同的拨动开关 |
| GEN-17 | 让 GitHub 更新地址连接失败后点击“检查更新” | 自动改查 Gitee；GitHub 正常返回时不请求 Gitee |
| GEN-18 | 切换通用、滚动和关于页 | 页面不显示整页滚动条；应用排除列表内容较多时只在列表内部滚动 |

## 按键与动作

| 编号 | 操作 | 预期结果 |
|---|---|---|
| BTN-01 | 鼠标移入录入区域后操作中键、按键 4、按键 5 | 无需点击开始；完成的操作自动加入或定位到映射列表 |
| BTN-02 | 在录入区域分别单击左键和右键 | 动态提示显示“左键和右键属于主按键，不可录入”；不新增映射，左键和右键保持原始行为 |
| BTN-03 | 分别录入单击、双击、长按、按住并滚动、按住并拖动 | 识别结果正确；操作从区域内开始后，移出区域再释放仍能完成录入 |
| BTN-04 | 录入一个已经存在的按键与操作组合 | 不新增重复行，已有行被定位并突出显示 |
| BTN-05 | 删除某个按键的最后一条映射 | 对应按键分组消失，该按键恢复原始行为 |
| BTN-06 | 分别用三键鼠标和五键以上鼠标首次启动 | 自动识别鼠标类型；三键鼠标获得 4 条中键默认值，五键以上鼠标获得按键 4/5 的 6 条默认值 |
| BTN-07 | 点击左下角“选项…”，依次选择双击速度“快”“中”“慢”并测试 | 扩展设置窗口正常打开；双击判断间隔分别为 150ms、250ms、400ms，重启后保留选择 |
| BTN-08 | 为没有双击映射的按键设置单击 | 单击立即执行，不等待双击判断时间 |
| BTN-09 | 新建至少两个虚拟桌面，将“虚拟桌面与任务视图”设为按住并拖动后分别向四个方向拖动 | 向左进入下一个桌面，向右进入上一个桌面，向上打开任务视图，向下显示桌面；每次切换后中键和两个侧键立即可用，可连续往返 |
| BTN-10 | 将“网页与标签导航”设为按住并拖动动作，分别向四个方向拖动 | 向左后退、向右前进、向上切到上一个标签页、向下切到下一个标签页 |
| BTN-11 | 查看三个映射分组 | 依次显示“中键、按键4（后退键）、按键5（前进键）”，不显示 MButton 或 XButton 名称 |
| BTN-12 | 逐条增加映射直到窗口同时接近屏幕上下边界 | 窗口先向下增长，接近底部后向上移动并继续增长；上下各剩约 16px 后停止变长并改为表内滚动，底部两个按钮始终可见 |
| BTN-13 | 在录入区域按住支持的鼠标键超过 500ms 后释放 | 无需移动或再次点击，自动新增“长按”映射并定位到该行 |
| BTN-14 | 在录入区域按住鼠标键并移动超过 12px | 自动识别为“点击并拖动”；不足 12px 时不误判为拖动 |
| BTN-15 | 将指针放在闭合的功能下拉框上滚动滚轮 | 当前功能值不变，映射表按滚轮方向滚动 |
| BTN-16 | 打开“选项…” | 只显示双击速度等识别选项，不提供方向选择；融合动作方向由程序固定 |
| BTN-17 | 分别新增点击、长按、双击、按住并拖动和按住并滚动映射 | 新映射立即获得与按键和操作匹配的默认功能，不显示“选择功能…” |
| BTN-18 | 删除某个按键的点击映射后单击该按键 | 单击保持 Windows 或当前应用的原始功能 |
| BTN-19 | 打开按钮页并检查映射表 | 页面无重复大标题；映射表占主要区域，每组固定按“点击、长按、双击、点击并拖动、点击并滚动”排列 |
| BTN-20 | 进入录入区并依次按下、长按、拖动、滚动和移出 | 加号下方依次显示当前录入状态，文字变化不会改变录入区尺寸或遮挡加号 |
| BTN-21 | 将鼠标移入录入框，再移出 | 移入时录入框轻微上浮并出现柔和阴影，移出后平滑恢复；动画不改变页面布局 |
| BTN-22 | 窗口因映射较多而上移后，逐条删除映射 | 窗口随内容缩短，但不会突然向下移动；列表不再溢出时滚动条自动消失 |
| BTN-23 | 打开普通操作映射的功能列表并选择第一项“执行原功能” | 第一项固定为“执行原功能”，触发后执行鼠标原始行为；“不执行动作”位于列表末尾 |
| BTN-24 | 分别打开普通操作、按住并滚动、按住并拖动的功能列表 | 普通操作完整显示通用动作；滚动列表只显示 6 个融合动作且不含快速滚动，拖动列表只显示 3 个融合动作，不包含普通单次动作 |

## Windows 动作与滚轮

| 编号 | 操作 | 预期结果 |
|---|---|---|
| ACT-01 | 测试 Back、Forward、Task View、显示桌面和开始菜单 | 每个动作执行一次，目标窗口保持正确 |
| ACT-02 | 测试切换虚拟桌面、`Alt + Tab` 和自定义快捷键 | 按键顺序正确，修饰键不会保持按下 |
| ACT-03 | 测试媒体控制、音量和缩放 | Windows 或当前应用执行对应动作 |
| ACT-04 | 分别测试音量调节、标签页切换、页面前进后退、虚拟桌面切换、桌面与开始菜单 | 一个融合动作根据滚轮上下方向执行对应的两个基础动作；桌面与开始菜单每个方向一次按住期间最多执行一次 |
| ACT-05 | 将开始菜单设为点击、双击或长按动作 | 对应操作成立后直接执行一次；长按、拖动和滚动只执行最先成立的操作 |
| ACT-06 | 将缩放或音量调节设为按住并滚动动作后连续滚动 | 每一格滚轮都按当前方向执行对应动作，反向滚动立即执行相反动作 |
| SCR-01 | 修改滚轮方向和速度 | 修改立即生效，方向与界面文字一致 |
| SCR-02 | 使用默认的 `Shift`、`Alt`、`Win`、`Ctrl` 配合滚轮 | 依次执行水平滚动、4 倍快速滚动、四分之一精确滚动、放大或缩小；松开按键后恢复普通滚动 |
| SCR-03 | 开启基础平滑，快速连续滚动 30 秒 | 滚动保持可控，不出现“hotkeys have been received”提示 |
| SCR-04 | 尝试把同一个修饰键分配给两个滚轮动作 | 后修改的一项自动恢复为关闭，并显示明确提示 |
| SCR-05 | 使用触摸板连续上下滑动页面 5 秒，再使用实体鼠标快速滚轮 | 不出现热键频率提示；触摸板保持系统滚动，实体鼠标滚轮和按住滚动动作仍可用 |

## 应用排除与全屏

| 编号 | 操作 | 预期结果 |
|---|---|---|
| EXC-01 | 从运行中的程序添加一个应用到排除列表 | 切入该应用后所有设置停止，鼠标保持原始行为 |
| EXC-02 | 移除排除应用或切回普通窗口 | 原有设置自动恢复，不需要重启程序 |
| EXC-03 | 开启“全屏时自动暂停”，进入全屏视频和游戏 | 全屏时鼠标保持原始行为，退出全屏后自动恢复 |
| EXC-04 | 保持“全屏时自动暂停”开启，切换到空虚拟桌面、任务视图和普通最大化窗口 | 不进入暂停状态，所有映射立即可用；回到真正的全屏应用后仍会暂停 |
| EXC-05 | 正在长按、移动或滚动时切入排除应用 | 当前动作立即结束，所有鼠标键和键盘修饰键均已释放 |
| EXC-06 | 快速来回切换排除应用与普通应用 | 状态稳定，不出现重复动作或持续按住状态 |

## 配置、恢复与发布包

| 编号 | 操作 | 预期结果 |
|---|---|---|
| CFG-01 | 修改开关、动作和滚轮设置 | 保存后立即生效，GUI 显示成功状态 |
| CFG-02 | 退出并重新启动程序 | 所有设置和排除列表保持不变 |
| CFG-03 | 修改设置并等待 400ms 以上 | AutoHotkey v2 核心自动读取新设置，不需要退出或重启 |
| CFG-04 | 使用无效或不完整的用户配置启动 | 程序保留上一份可用设置或使用默认值，并显示清楚提示 |
| CFG-05 | 使用 `configVersion: 1` 且含 `buttons` 的旧配置启动 | 设置自动迁移为 `configVersion: 2` 的 `remaps`；重写后不再含顶层 `buttons` |
| CFG-06 | 在旧配置中为不同移动和滚动方向设置不同动作后启动 | 旧动作以兼容映射保留，不因迁移而静默消失 |
| CFG-07 | 删除或修改映射后点击“恢复默认值...” | 弹出三键鼠标和五键或多键鼠标两项；选择后恢复对应 4 条或 6 条默认值并立即生效 |
| CFG-08 | 临时使用户配置目录不可写后修改设置 | 显示保存失败原因，界面与核心恢复到最后一次成功保存的状态；恢复写入条件后可再次保存 |
| CFG-09 | 临时移走核心程序后打开通用页拨动开关 | 显示核心启动失败提示，拨动开关恢复关闭状态；核心恢复后可正常打开 |
| CFG-10 | 从通用页底部打开“配置管理…”并新建配置副本 | 新配置出现在列表中并成为当前配置，原配置保持不变 |
| CFG-11 | 在两套配置之间切换并重新启动程序 | 切换前自动保存，重启后继续使用最后选择的配置 |
| CFG-12 | 导出当前配置，再以新名称导入 | 导入后的设置完整保留并成为当前配置 |
| CFG-13 | 创建备份、修改设置并恢复备份 | 恢复前自动保存当前状态；恢复后按钮、滚轮、应用排除和 Beta 开关回到备份值 |
| CFG-14 | 重命名和删除配置档案 | 名称立即更新；删除当前配置时自动切换到剩余配置，最后一个配置不能删除 |
| PKG-01 | 分别在 Windows 10 22H2 x64 和 Windows 11 x64 安装并启动安装版 | 安装程序正确检查 .NET Framework 4.8；无需另装 AutoHotkey，GUI 与鼠标功能均可运行 |
| PKG-02 | 安装后打开 Windows 开始菜单并搜索“Win Mouse Fix” | “所有应用”和搜索结果中均能找到并启动 Win Mouse Fix |
| PKG-03 | 修改设置后，直接安装更高版本到同一路径 | 程序文件更新为新版本，开始菜单入口仍可用，`%AppData%\WinMouseFix` 中的设置保持不变 |
| PKG-04 | 开启登录运行后卸载程序 | 程序文件、开始菜单入口和登录运行登记被移除；`%AppData%\WinMouseFix` 中的设置保留 |
| PKG-05 | 复制新版 `dist/app` 覆盖旧便携目录后启动，再从托盘退出并删除目录 | `%AppData%\WinMouseFix` 中的用户设置保留；退出后没有残留进程，发布目录可删除 |
| PKG-06 | 运行 `build.ps1` 并检查体积报告 | GUI 主程序及其依赖合计不超过 10 MiB；鼠标核心和 `config/` 不计入该数值，发布目录不重复包含界面图标 |
| PKG-07 | 安装时查看目标目录页，浏览并选择当前用户可写的自定义目录 | 默认目录为 `%LocalAppData%\Programs\Win Mouse Fix`；可成功修改目录，安装后程序、开始菜单入口和卸载功能均正常 |

## 结果

- [ ] 全部必测项目通过
- [ ] 失败项目已有复现步骤和截图或录屏
- [ ] `.\scripts\validate.ps1` 返回 `0`
- [ ] `.\scripts\build.ps1` 生成可运行的 `dist/app` 发布目录
